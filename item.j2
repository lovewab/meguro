{% from "macros.j2" import render_subject_content %}

{% block style %}
  <link href="https://fonts.googleapis.com/css?family=Roboto:300,400&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="{{ static_url }}src/index.css">
{% endblock %}

{% block body %}
{% include "gh:typlog/meguro/_head.j2" %}
<div class="main">
  <div class="inner">
    <article role="main" class="h-entry" itemscope itemtype="http://schema.org/Article">
      <h1 class="p-name" itemprop="headline">{{ page.title }}</h1>
      {% if page.subtitle %}
        <h2 class="p-summary">{{ page.subtitle }}</h2>
      {% endif %}
      <div class="tags">
        {% for tag in page.tags %}
          <a class="{{ tag.slug }}" href="{{ tag.url }}"{% if tag.meta.color %} style="--color:{{tag.meta.color}}"{% endif %}>{{ tag.title }}</a>
        {% endfor %}
      </div>
      <div class="e-content js-content yue" itemprop="articleBody">
        {{ render_subject_content(site, page) }}
      </div>
    </article>
  </div>
</div>
{% endblock %}
